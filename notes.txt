
To Do:

Find or write efficient implementations for bitwise ops: LSB, MSB, Population Count

Implement Make/Unmake

If most moves are usually going to be spawned in parallel, this would substantially reduce the benefit of generating moves concurrently in batches.

Disable boundary checking on compile




Debugging:

furthest_forward() returns out of range value when passed a zero bitboard.

board state is getting corrupted:
